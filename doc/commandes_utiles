# Commandes utiles

git config commit.template .gitmessage

## Corriger une migration avant commit

- git co db/schema.rb # pour annuler les modifications en base du point de vue activerecord
- rails db:drop db:setup || rails db:migrate
curl -F  fichier=@test/fixtures/files/nomenclature_simple.xml http://localhost:3000/fichiers_xml
